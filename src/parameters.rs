pub struct Parameters {
    pub amount: usize,
    pub border: f32,
    pub timestep: f32,
    pub gravity_constant: f32,
    pub friction: f32,
    pub mass_red: f32,
    pub mass_green: f32,
    pub mass_blue: f32,
    pub max_velocity: f32,
    pub database_path: String,
    pub bucket_size: f32,
}
